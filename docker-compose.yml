services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: opscenter
      POSTGRES_PASSWORD: opscenter
      POSTGRES_DB: opscenter
    ports:
      - "5432:5432"
    volumes:
      - opscenter_db:/var/lib/postgresql/data

volumes:
  opscenter_db:
